import { UtilisateurService } from './utilisateur.service';

describe('UtilisateurService', () => {
  it('should create an instance', () => {
    expect(new UtilisateurService()).toBeTruthy();
  });
});
