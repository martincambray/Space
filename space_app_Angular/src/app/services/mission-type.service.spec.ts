import { MissionTypeService } from './mission-type.service';

describe('MissionTypeService', () => {
  it('should create an instance', () => {
    expect(new MissionTypeService()).toBeTruthy();
  });
});
