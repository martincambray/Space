import { MissionStatusModel } from './mission-status.model';

describe('MissionStatusModel', () => {
  it('should create an instance', () => {
    expect(new MissionStatusModel()).toBeTruthy();
  });
});
