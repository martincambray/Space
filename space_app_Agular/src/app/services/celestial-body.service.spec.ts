import { CelestialBodyService } from './celestial-body.service';

describe('CelestialBodyService', () => {
  it('should create an instance', () => {
    expect(new CelestialBodyService()).toBeTruthy();
  });
});
