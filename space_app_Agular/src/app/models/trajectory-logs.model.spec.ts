import { TrajectoryLogsModel } from './trajectory-logs.model';

describe('TrajectoryLogsModel', () => {
  it('should create an instance', () => {
    expect(new TrajectoryLogsModel()).toBeTruthy();
  });
});
