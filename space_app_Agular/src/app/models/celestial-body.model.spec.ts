import { CelestialBodyModel } from './celestial-body.model';

describe('CelestialBodyModel', () => {
  it('should create an instance', () => {
    expect(new CelestialBodyModel()).toBeTruthy();
  });
});
